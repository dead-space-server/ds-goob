These files contain upstream server rules retained with the codebase. Dead Space Goob uses its own
configured rules file; do not enable these files for players without first reviewing their contents.

The filenames used for the rules files are not themselves rules. Only the contents of the files are rules.
